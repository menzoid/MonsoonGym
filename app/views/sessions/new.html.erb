<div class="container">
  <%= simple_form_for [ @training, @session] do |f| %>
  <%= f.association :member, label_method: :first_name%>
  <%= f.button :submit, wrapper_html: { class: 'btn btn-primary' }%>
  <% end %>
</div>

<%= link_to "Back to class", training_path(@training) %>
